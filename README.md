# Media Collection Manager

Private-use desktop media manager developed iteratively with ChatGPT.

## Updates

The application reads `latest.json` from this repository and can download verified update archives from `releases/`.

Update packages are verified with SHA-256 before installation. User database, thumbnails and settings live outside the application folder in `~/.media_collection_manager` and are not replaced by application updates.
